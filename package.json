{
  "name": "gdsc",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "clean": "rm -rf .next && rm -rf dist && npm run build",
    "build": "next build && touch dist/.nojekyll",
    "start": "next start",
    "predeploy": "npm run clean && npm run build",
    "deploy": "gh-pages -d dist -b gh-pages"
  },
  "dependencies": {
    "babel-plugin-styled-components": "^2.1.4",
    "next": "^14.2.24",
    "react": "^18",
    "react-dom": "^18",
    "styled-components": "^6.1.12"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "@types/styled-components": "^5.1.34",
    "eslint": "^8",
    "eslint-config-next": "14.2.5",
    "gh-pages": "^6.1.1",
    "typescript": "^5"
  }
}
